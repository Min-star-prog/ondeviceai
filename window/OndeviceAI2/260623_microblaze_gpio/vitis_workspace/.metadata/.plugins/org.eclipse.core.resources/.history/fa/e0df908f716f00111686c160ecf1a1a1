#include <stdint.h>
#include "sleep.h"
#include "xil_printf.h"
#include "driver/LED/LED.h"

void FND_Init()
{
	uint32_t fndComTemp = GPIO_GetCR(GPIOB);
	GPIO_SetMode(GPIOA, 0xff);
	fndComTemp |= 0x0f;
	GPIO_SetMode(GPIOB, fndComTemp);
}


int main()
{
	int counter = 0;
	uint16_t ledState = 1;

	LED_Init();

	while(1)
	{
		xil_printf("counter = %d\n", counter++);

//		ledState = (ledState << 1) | (ledState >> 15);	//¿ÞÂÊ shift
//		ledState = (ledState >> 1) | (ledState << 15);	//¿À¸¥ÂÊ shift
//		LED_WritePort16(ledState);
//		LED_PinToggle(15);
		LED_PinOff(15);
		usleep(100000);
	}
	return 0;
}

